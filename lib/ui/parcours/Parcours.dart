import 'package:flutter/material.dart';

class ParcoursSection extends StatelessWidget {
  const ParcoursSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
